void BubbleSort(int a[], int n)
{
	int temp;
	for (int i = 0; i < LEN; i++)
		for (int j = i + 1; j < LEN; j++)
		{
			if (a[i] >= a[j])
			{
				temp = a[i];
				a[i] = a[j];
				a[j] = temp;
			}
		}
}
